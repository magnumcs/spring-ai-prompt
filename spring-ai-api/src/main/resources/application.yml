spring:
  application:
    name: spring-ai-api
  ai:
    openai:
      #api-key: ${OPEN_AI_API_KEY}
      api-key: ${DEEP_SEEK_API_KEY}
      base-url: https://api.deepseek.com/
      chat:
        options:
          model: "deepseek-chat"
          #model: "gpt-4o"